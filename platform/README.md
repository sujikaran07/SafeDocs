# SafeDocs Platform & Engine

Welcome to the **SafeDocs** project. This repository contains both the document analysis platform and the back-end engine.

## 📂 Project Structure

- **`platform/`** (Root for UI): Next.js (App Router) web application.
- **`engine/`**: Python-based analytical core (located in the parent directory).

## 🚀 Commands (from within `platform/`)

1.  **Start Platform**:
    ```bash
    npm run dev
    ```
2.  **Start Engine**:
    ```bash
    npm run engine:dev
    ```

---
© 2025 SafeDocs Team.
